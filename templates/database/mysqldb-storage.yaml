apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "datashield.mysqldb.name" . }}
  labels:
    name: {{ include "datashield.mysqldb.name" . }}
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.mongodb.pvc.storage | quote }}
  volumeName: {{ include "datashield.mysqldb.name" . }}

  
  
