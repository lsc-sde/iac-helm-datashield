apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-storage-claim-1
  labels:
    name: mongo-storage-claim-1
    environment: {{ .Values.environment }}
    context: {{ .Values.context }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: mongo-storage-volume-1
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: manual
